# Legacy — v1 vanilla-JS app

Retired Sept 2026 after the React migration (`apps/web/`).
Kept as a reference/safety net. Data source of truth for the converter
(`apps/web/scripts/convert-data.mjs`) is still `../../data/*.js`.
